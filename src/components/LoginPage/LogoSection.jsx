import { Iconspoid } from '../../utils/icons';
import { Logo } from '../../utils/icons';

const LogoSection = () => {
  return (
    <>
      <div>Logo</div>
    </>
  );
};
export default LogoSection;
